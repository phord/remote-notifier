{
  "name": "remote-notifier-shared",
  "version": "1.0.0",
  "private": true,
  "main": "./types.ts",
  "types": "./types.ts",
  "scripts": {
    "build": "echo 'shared types - no build needed'"
  }
}
